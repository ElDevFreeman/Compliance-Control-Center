@inject OEA.Checklist.Web.State.ThemeState Theme
<!DOCTYPE html>
<html lang="es-MX" data-theme="@Theme.Current">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <link rel="stylesheet" href="css/site.css" />
    <link rel="stylesheet" href="OEA.Checklist.Web.styles.css" />
    <link rel="icon" type="image/png" href="favicon.png" />
    <title>Checklist OEA · Auditoría Mensual</title>
    <HeadOutlet />
    <script>
        // Aplica el tema desde la cookie/localStorage antes de que Blazor pinte,
        // para evitar cualquier flash de tema incorrecto.
        (function () {
            try {
                var m = document.cookie.match(/(?:^|;\s*)oea_theme=([^;]+)/);
                var t = m ? decodeURIComponent(m[1]) : (localStorage.getItem("oea_theme") || "emerald");
                if (t !== "emerald" && t !== "night") t = "emerald";
                document.documentElement.setAttribute("data-theme", t);
            } catch (e) { }
        })();
    </script>
</head>

<body class="min-h-screen bg-base-200 text-base-content">
    <Routes />
    <script src="_framework/blazor.web.js"></script>
    <script src="js/app.js"></script>
</body>

</html>
